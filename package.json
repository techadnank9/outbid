{
  "name": "outbid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Outbid \u2014 pay-to-rank leaderboard. Real bids, real payments, real click tracking.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "build": "node build.js",
    "deploy": "./deploy.sh outbid",
    "deploy:dethrone": "./deploy.sh dethrone",
    "deploy:all": "./deploy.sh outbid && ./deploy.sh dethrone && ./deploy.sh socialrise",
    "deploy:socialrise": "./deploy.sh socialrise"
  }
}
