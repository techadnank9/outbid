{
  "name": "outbid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pay-to-rank leaderboard. Real bids, real payments, real click tracking.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "build": "node build.js"
  }
}
